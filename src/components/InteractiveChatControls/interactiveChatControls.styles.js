const styles = {
  'interactive-chat-controls': {
    borderTop: (theme) => '1px solid ' + theme.palette.divider,
    padding: (theme) => ({
      xs: theme.spacing(2),
      sm: theme.spacing(4),
    })
  }
};

export default styles;